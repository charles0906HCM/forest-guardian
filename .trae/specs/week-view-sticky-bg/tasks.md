# Tasks

- [x] Task 1: 为周视图 sticky 元素添加不透明背景
  - [x] 星期标题行添加不透明背景（`bg-white/90 backdrop-blur-sm`）
  - [x] 时间列使用不透明背景（`bg-white/90 backdrop-blur-sm`）
  - [x] 左上角"时间"单元格使用不透明背景（`bg-white/90 backdrop-blur-sm`）
  - [x] 选中日标题在整行 bg-white/90 背景上叠加，也不透明
  - [x] 验证：上下滚动时表头文字不叠加
  - [x] 验证：左右滑动时时间列文字不叠加
  - [x] 验证：左上角"时间"两个方向滚动都不叠加

# Task Dependencies
- 无依赖，单一任务
