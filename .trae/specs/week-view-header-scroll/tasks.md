# Tasks

- [x] Task 1: 合并周视图表头和主体到同一滚动容器
  - [x] 将星期标题行和主体区域（时间列+7天列）放入同一个 `overflow-auto` 容器
  - [x] 星期标题行使用 `sticky top-0 z-20` 固定在顶部
  - [x] 时间列标题格（"时间"）同时使用 `sticky top-0 left-0 z-30` 固定在左上角
  - [x] 时间列小时刻度保持 `sticky left-0 z-10` 固定在左侧
  - [x] 确保左右滑动时表头与内容同步移动
  - [x] 确保上下滑动时表头固定不动

# Task Dependencies
- 无依赖，单一任务
